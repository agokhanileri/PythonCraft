# ========================================
# Enforce Unix LF for consistency
# ========================================
* text=auto eol=lf

# Markdown / Docs as normalized text
*.md text eol=lf
*.txt text eol=lf

# Python: enforce LF
*.py text eol=lf

# Shell / scripts enforce LF
*.sh text eol=lf
*.bash text eol=lf

# ========================================
# Optional binary hints for Git LFS
# Uncomment if using Git LFS later
# ========================================
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# *.mp3 filter=lfs diff=lfs merge=lfs -text
# *.zip filter=lfs diff=lfs merge=lfs -text
# *.png filter=lfs diff=lfs merge=lfs -text
